# Requirements for pipeline/step1_build_siglip_rag.py
#                   pipeline/step2_run_baseline.py
#                   pipeline/step3_evaluate.py
#
# Do NOT install manually — use remote_setup.sh which handles
# arch-aware PyTorch installation before this file is processed.

# ── PyTorch ───────────────────────────────────────────────────────────────────
# Installed arch-aware by remote_setup.sh (aarch64 → PyPI, x86_64 → cu128 whl).
# Listed unpinned here so pip satisfies transitive deps without overriding the
# already-installed wheel with a wrong-platform build.
torch
torchvision

# ── HuggingFace stack ─────────────────────────────────────────────────────────
transformers==4.57.6        # SiglipModel, AutoModelForImageTextToText, BitsAndBytesConfig
accelerate==1.13.0          # required for device_map="auto" + quantisation
bitsandbytes==0.49.2        # 4-bit NF4 quantisation (step 2)
safetensors==0.7.0
huggingface_hub==0.36.2
tokenizers==0.22.2
sentencepiece==0.2.1

# ── Qwen VL ───────────────────────────────────────────────────────────────────
qwen-vl-utils               # Qwen3-VL processor utilities

# ── Numerics / vision ─────────────────────────────────────────────────────────
numpy                       # unpinned — let torch resolve the right platform wheel
pillow==12.1.1

# ── Evaluation metrics (step 3) ───────────────────────────────────────────────
scikit-learn==1.8.0
scipy==1.17.1

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.67.3
packaging==26.0
regex==2026.3.32
requests==2.33.1
filelock==3.25.2
Jinja2==3.1.6
typing_extensions==4.15.0